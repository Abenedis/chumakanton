# 📋 Пошаговая инструкция деплоя на Railway

## Шаг 1: Инициализация Git и загрузка в GitHub ✅

Выполните следующие команды в терминале:

```bash
cd /Users/alexbenedis/1

# Инициализация git (если еще не сделано)
git init

# Добавление всех необходимых файлов
git add .gitignore README.md app.py requirements.txt Procfile railway.json README_RAILWAY.md

# Создание первого коммита
git commit -m "Initial commit: Flask API for floor plan generation"

# Переименование ветки в main
git branch -M main

# Добавление remote репозитория
git remote add origin https://github.com/Abenedis/chumakanton.git

# Отправка в GitHub
git push -u origin main
```

**Примечание:** Если remote уже существует, используйте:
```bash
git remote set-url origin https://github.com/Abenedis/chumakanton.git
```

---

## Шаг 2: Деплой на Railway

### 2.1. Создание проекта на Railway

1. Зайдите на [railway.app](https://railway.app)
2. Войдите через GitHub (используйте тот же аккаунт, что и для репозитория)
3. Нажмите **"New Project"**
4. Выберите **"Deploy from GitHub repo"**
5. Выберите репозиторий: **`Abenedis/chumakanton`**
6. Railway автоматически:
   - Определит Python проект
   - Установит зависимости из `requirements.txt`
   - Запустит через `Procfile`

### 2.2. Ожидание деплоя

- Railway начнет автоматический деплой
- Процесс займет 2-5 минут
- Вы увидите логи установки зависимостей и запуска

### 2.3. Получение URL

После успешного деплоя:
1. В настройках проекта Railway нажмите на **"Settings"**
2. Найдите раздел **"Domains"**
3. Скопируйте URL (например: `https://your-app-name.railway.app`)
4. Или Railway автоматически создаст домен

---

## Шаг 3: Обновление Flutter кода

После получения Railway URL:

1. Откройте файл:
   ```
   flutter_module_chumak/lib/services/floor_plan_service.dart
   ```

2. Обновите URL:
   ```dart
   static const String railwayUrl = 'https://your-app-name.railway.app'; // Ваш Railway URL
   static const bool useLocalhost = false; // false для production
   ```

3. Выполните:
   ```bash
   cd flutter_module_chumak
   flutter pub get
   ```

---

## Шаг 4: Проверка работы

1. Откройте Railway URL в браузере
2. Должна открыться страница загрузки JSON
3. Проверьте endpoint: `https://your-app-name.railway.app/convert`
4. Запустите iOS приложение и протестируйте

---

## 🐛 Решение проблем

### Проблема: Git push не работает

**Решение:**
- Проверьте, что вы авторизованы в GitHub
- Используйте: `git push -u origin main --force` (если нужно перезаписать)

### Проблема: Railway не находит проект

**Решение:**
- Убедитесь, что репозиторий публичный или у вас есть доступ
- Проверьте, что все файлы закоммичены

### Проблема: Деплой падает

**Решение:**
- Проверьте логи в Railway
- Убедитесь, что `requirements.txt` содержит все зависимости
- Проверьте, что `Procfile` правильный

---

## ✅ Чеклист

- [ ] Git репозиторий инициализирован
- [ ] Все файлы добавлены и закоммичены
- [ ] Код отправлен в GitHub
- [ ] Проект создан на Railway
- [ ] Деплой успешно завершен
- [ ] Railway URL получен
- [ ] Flutter код обновлен с Railway URL
- [ ] Приложение протестировано

---

## 🎉 Готово!

После выполнения всех шагов ваш Python сервер будет доступен по Railway URL и готов к использованию с iOS приложением!

