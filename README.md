# Floor Plan Generator API

Flask API для генерации планов этажей из данных Room Plan (iOS).

## 🚀 Быстрый старт

### Локальная разработка

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python3 app.py
```

Сервер запустится на `http://localhost:5000`

### Деплой на Railway

1. Проект автоматически деплоится при push в GitHub
2. Railway определит Python проект и установит зависимости
3. Сервер будет доступен по URL, предоставленному Railway

## 📁 Структура проекта

- `app.py` - Основной файл Flask приложения
- `requirements.txt` - Python зависимости
- `Procfile` - Команда запуска для Railway
- `railway.json` - Конфигурация Railway

## 🔌 API Endpoints

### POST `/convert`

Генерирует план этажа из JSON данных Room Plan.

**Request:**
```json
{
  "json_data": "...",  // JSON строка с данными Room Plan
  "wall_line_width": 22  // Толщина стен в пикселях
}
```

**Response:**
```json
{
  "success": true,
  "image": "base64_encoded_png_image",
  "stats": {
    "walls": 4,
    "doors": 2,
    "windows": 3,
    "rooms": 1,
    "perimeter": 25.5
  }
}
```

## 🛠 Технологии

- Flask 3.0.0
- Matplotlib - для генерации изображений
- NumPy, SciPy, Shapely - для геометрических вычислений
- Flask-CORS - для поддержки CORS запросов

## 📝 Лицензия

См. LICENSE файл

